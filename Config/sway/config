# Sway configuration — main entry point
# Individual settings are in conf.d/

# Mod key (Mod4 = Super/Win)
set $mod Mod4

# Default terminal
set $term kitty
set $term2 foot

# Drag floating windows with Mod+mouse
floating_modifier $mod

include conf.d/*.conf
